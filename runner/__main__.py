"""Entry point for `python -m forge_runner`.

cli.py is implemented in Phase 2 (T027).  This stub imports it so that the
package is importable and the entry point is wired correctly once cli.py lands.
"""

from .cli import main  # noqa: E402 — cli.py arrives in Phase 2

main()
